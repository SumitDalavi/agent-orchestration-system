version: '3.8'
services:
  server:
    build:
      context: ./server
    ports:
      - "4007:4007"
    environment:
      - PORT=4007

  client:
    build:
      context: ./client
    ports:
      - "8087:80"
    depends_on:
      - server
